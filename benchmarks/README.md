Check the preprint for a theoretical discussion on the i.i.d. model.\
Here is the result of a *very* minimal benchmark (for what it is worth).\ I ran `/main/sparse_dp_k` with `k` such that `k in Theta(log(nm))`, and compared it against a suffix tree implementation ().

![suffix_tree_vs_sparse_dp_k](/benchmarks/pic.png)
